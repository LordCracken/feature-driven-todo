import { Typography } from '@mui/material';

const App = () => {
  return (
    <Typography variant="h3" component="h1" sx={{ textAlign: 'center', fontSize: { xs: '1.5rem', md: '3rem' } }}>
      Feature-Driven Todos
    </Typography>
  );
};

export default App;
